class CreateBranch {
    constructor(branch_code, branch_name, region, address) {
        this.branch_code = branch_code;
        this.branch_name = branch_name;
        this.region = region;
        this.address = address;
    }
}

module.exports = CreateBranch;
